# Building assets completion audit

The authorized deliverables are 60 new buildings, 12 assembled sites, 24 supporting assets, and reusable local/CI capture automation for the complete game catalog. This audit does not authorize publication or district rollout.

| Requirement | Evidence | Current finding |
| --- | --- | --- |
| B01–B60 exist as distinct owned packages | `src/world/data/buildings/`; explicit numbered-roster assertions in `src/world/assetExpansion.test.ts` | All 60 codes occur exactly once and every package has a yard context. |
| S01–S12 exist as assembled sites | `src/world/data/sites/neighborhoods/`; named-site assertions in `assetExpansion.test.ts` | All twelve sites exist and are discoverable. Placement checks cover overlaps, limits and canopy clearance. |
| D01–D24 have registered implementations | Detail-host, prop/canopy and fixture-context assertions in `assetExpansion.test.ts` | All 24 are present through their appropriate production paths. |
| Assets use actual game rendering and destruction | `src/debug/assetCapture.ts`, production constructors, `WorldRenderer`, and `stepWorld` | Capture uses the production implementation with deterministic simulation steps. No replacement artwork is generated. |
| Complete catalog and variant discovery | Capture catalog derived from `discoverYardAssets`, plus mounted-detail and runtime-vehicle hosts | 193 playable yard entries plus ten detail hosts and two runtime-vehicle contexts; 205 contexts / 239 variants total. |
| Intact model, layers, floors and destruction subfolders | `scripts/capture-assets.mjs` | Standard views include each layer isolated/disabled, floor cutaways/structures, overlays, damage stages and eleven timed destruction stages in three views. Optional exhaustive mode generates 4,096 layer combinations. |
| Damage and restoration verification | Runner assertions and per-image state/hashes | Each run checks applicable destruction, attached-detail/tank/silo cleanup and exact intact-image restoration. Runtime vehicles document their absent destruction mechanic and use motion sequences. |
| Gallery completeness and integrity | `scripts/verify-asset-captures.mjs` and its 13 passing tests | Missing variants/views, duplicate paths, altered images, incomplete restoration and non-runtime vehicle leakage are rejected. |
| Complete corrected capture run | `artifacts/catalog-isolated/` | **Passed:** all four corrected shards exited successfully; the strict verifier confirmed 205 contexts, 239 variants and 16,582 screenshots with zero errors. `artifacts/catalog-isolated/verification.json` records the result. Earlier galleries are superseded. |
| Simulation, package and rendering regression | Full Vitest result recorded in `building-asset-progress.md` | 488 tests in 59 files passed after residential circulation work. Two subsequent explicit roster gates passed in the four-case expansion run. The later capture-only visibility change passed typecheck, build and a 79-view grain-elevator capture. |
| Playable yard | `artifacts/verify-current-yard.mjs`, `artifacts/current-yard-playable.png` | 193 entries loaded with zero registry/browser errors; keyboard input moved the bulldozer. |
| Visual quality | Individual checkpoints in `building-asset-progress.md` | Intact, cutaway and destruction reviews found and corrected stair ordering, oversized furnishings, roof details and capture isolation. Representative corrected-run review passed for structural collapse, fixture hosts and mounted details, including the final roof-duct, dormer-cleanup and clock-face views. No unresolved defect remains from these reviews. |
| Automated CI delivery | `.github/workflows/asset-captures.yml` | Four capture jobs install Chromium and build; verification checks their combined artifacts and uploads the gallery. The same capture/verifier commands have run locally. Hosted Actions execution is unverified because changes have not been committed or pushed. |
| Preserve existing gameplay distribution | Zero zoning weights in new packages; district/migration regression; yard-only registration | Existing district selection is preserved. New normal-district placement is outside this asset-generation objective. |
| Avoid committing artifacts | `.gitignore`; worktree remains uncommitted | Screenshots, manifests, local linked configuration and build output stay outside the intended source change set. |

The local implementation and capture deliverables are complete and verified. The accepted gallery is `artifacts/catalog-isolated/index.html`. All 13 verifier tests passed again after combined verification. Hosted CI execution is not claimed: the workflow is ready for publication, but no commit or push was authorized or performed. Screenshots and builds remain ignored local artifacts.
